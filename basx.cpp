#include "basx.h"

template<class T>
basex<T>::basex(T t, int b): raw_number(t), base(b) {
    //se base>1 ok altrementi err
};

template<class T>
basex<T>::basex(const string& s,int b):  {
    //se base>1 ok altrementi err
    //controllo num<0
    //chiamata string->T
};
//conversione da string a base x







